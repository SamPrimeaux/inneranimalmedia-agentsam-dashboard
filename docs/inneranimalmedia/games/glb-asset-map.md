# GLB asset map

## Storage

- Meshy / 3D exports: prefer `AUTORAG_BUCKET` (`autorag`) under `meshy/...` per `AGENTS.md` (CAD_ASSETS binding removed).

## Delivery

- Worker and dashboard GLB viewer paths should match keys actually uploaded; verify R2 listing after publish.

## Conventions

- Document model id, source tool, and license in this file or linked index when adding assets.
