# Chess architecture

## Worker

- `CHESS_SESSION` Durable Object namespace is bound for future/public chess product; keep connected per infrastructure docs.

## Client

- Any future dashboard routes should use the same auth and API patterns as the rest of the platform.

## Notes

- Low priority relative to Agent Sam core; coordinate before large schema or DO changes.
