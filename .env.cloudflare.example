# Copy to .env.cloudflare (gitignored). Used by scripts/with-cloudflare-env.sh.
#
# Minimal set for deploy / R2 / D1 from your machine:
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# Full secret and var inventory for recovering Workers: see repo root .env.example
