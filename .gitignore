# Env / secrets — never commit
.env
.env.local
.env.*.local
.env.cloudflare
.env.secrets.recovery

# AppleDouble / macOS metadata
._*
.DS_Store
**/.DS_Store

# Cursor AI (local editor config; MCP token lives in ~/.cursor/mcp.json for prod)
.cursor/

# Dependencies
node_modules/

# Wrangler
.wrangler/
.dev.vars

.dev.vars*
!.dev.vars.example
!.env.example

# Built artifacts — deploy via R2, not git
agent-dashboard/dist/
agent-dashboard/agent-dashboard/dist/
agent-dashboard/dist-debug/

# Cursor AI index binaries
.git/cursor/

# Deploy temp logs
/tmp/deploy-*.log

# Local overnight / canary JSON (may reference sessions)
reports/

# Local reports / overnight outputs
*.log

# Local marker: last git HEAD used for incremental doc uploads in deploy-with-record.sh
.deploy-docs-baseline
*.tmp
*.bak*
audit-*.txt

# One-off audit / analysis output dirs
audit/

# Source backup copies (not deployed)
agentsam-clean/

# macOS metadata junk
migrations/._*
**/.DS_Store
._*
# macOS metadata
._*
.DS_Store
# macOS metadata
._*
.DS_Store
paperclip

# Supabase CLI — local link cache (versions, project-ref, pooler URL); never commit
supabase/.temp/

# Design Studio local CAD scratch (keep README only)
local/designstudio/*
!local/designstudio/README.md
