# Terminal / PTY lock-down (2026-03-11)

Terminal is working (Worker TERMINAL_SECRET + TERMINAL_WS_URL; iam-pty behind tunnel). Do not change these without Sam approval.

## PERMANENT DO NOT TOUCH LIST

- **~/.cloudflared/config.yml** — NEVER add protocol: http2
- **~/iam-pty/ecosystem.config.cjs** — NEVER change without Sam approval
- **~/Library/LaunchAgents/** — NEVER create plist files for iam-pty or terminal
- **~/Downloads/march1st-inneranimalmedia/server/terminal.js** — DELETE THIS FILE if present; it conflicts with iam-pty
- **~/.local/iam-terminal-server/** — DELETE THIS DIRECTORY if present; it conflicts with iam-pty
- **wrangler.production.toml** — already protected (binding change kills the site)
- **worker.js** — handleGoogleOAuthCallback / handleGitHubOAuthCallback — already protected
